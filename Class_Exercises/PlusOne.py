# def plus_one(numbers):
#
#     for number in range(0, len(numbers), 1):
#         if len(numbers) == 1 and numbers[0] == 9:
#             numbers = [1, 0]
#
#         if number == len(numbers) - 1:
#             last_element = numbers[-1] + 1
#             if last_element == 9:
#                 numbers = len(numbers) + 1
#                 last_element = numbers[-2] + 1
#             numbers[-1] = last_element
#
#     return numbers
#
#
# given = [4, 9, 9]
# given2 = [9]
#
# print(plus_one(given2))
# print(plus_one(given))

def plus_one(numbers):
    check = ""
    numbers = check

